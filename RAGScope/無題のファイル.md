「Phase 3」は正式なプロジェクト管理上の状態名ではなく、今回の再計画で使っていた作業上の呼称です。具体的には、**RS-0014に着手して共通エラー・ログ契約を設計する段階**を指します。

現在地からの作業を、正式なTicket中心で並べ直すと次のとおりです。

1. RS-0014へ着手する

`status`を`planned`から`in_progress`へ変更し、次を作成・改訂します。

- `エラー・ログ設計.md`を技術非依存の共通契約へ改訂
- 構造化ログのJSON Schemaを作成
- Schema適合・不適合fixtureを作成
- `execution_id`の生成・伝播規則を確定
- execution／service／diagnostic eventの境界を確定
- logger自身が失敗した場合の意味を確定
- ADR-0001への現行参照をADR-0002へ切り替え
- Schema、fixture、設計書間の整合確認

この段階ではHaskell・Pythonの実装やHTTP API実装は行いません。

2. RS-0014の成果物をレビューする

特に次を確認します。

- 共通契約が特定言語へ依存していないか
- JSON Schemaを両方のコンポーネントから利用できるか
- logger失敗時の扱いが実装可能か
- `execution_id`の範囲がCLI command単位になっているか
- HTTP固有事項をRS-0012へ適切に残しているか

問題がなければRS-0014を完了させます。

3. RS-0015を実装する

Haskell側に共通エラー・構造化ログ基盤を実装します。

- 共通エラー型
- event・Context
- JSON encoder
- stderr sink
- テスト用sink
- Schema・fixture適合テスト
- logger失敗時のテスト

4. RS-0018を実装する

Python AI Service側にも同じ契約を利用する最小基盤を実装します。

- Python例外を共通エラーへ変換する境界
- 想定外例外のfallback
- event・Context
- JSON出力
- stderr sink
- テスト用sink
- Schema・fixture適合テスト

Web frameworkやEmbedding固有例外のmappingは、ここでは実装しません。

5. 共通エラー・ログEpicを完了する

RS-0014・RS-0015・RS-0018について、設計・Schema・fixture・各実装に矛盾がないことを確認します。

6. RS-0011へ戻る

既存の`文書処理設計.md`を保持したまま、次を追加して完成させます。

- 文書処理固有のerror code
- 読み込み・チャンク化の開始／成功／失敗event
- 共通エラー契約との対応
- 自動retryを行わない判断と理由

7. 文書取り込みを実装する
    - RS-0001：固定Markdownファイルの読み込み
    - RS-0002：固定ルールによるチャンク化
8. Embedding生成と保存へ進む

おおむね次の順序です。

- RS-0012：Embedding生成、OpenAPI、データモデルの設計
- RS-0016：retry / timeoutの機能設計
- RS-0017：retry executorとtimeout制御
- RS-0003：Python AI ServiceのEmbedding生成API
- RS-0004：Haskell側のHTTP client
- RS-0005・RS-0006：PostgreSQL / pgvectorへの保存

今回追加したHTTP上の`execution_id`伝播と機能固有例外mappingは、RS-0012・RS-0003・RS-0004で具体化します。

したがって、直近の次作業はシンプルに言うと、**RS-0014を`in_progress`にして、共通エラー・ログ設計、JSON Schema、fixtureの作成に着手すること**です。

---
はい。RS-0014の中心作業は、既存の`docs/design/エラー・ログ設計.md`を修正することです。同名ファイルを新規作成するのではなく、現在のファイルをそのまま改訂します。

ただし、設計書だけでは完了せず、次の3種類が成果物になります。

1. `エラー・ログ設計.md`の改訂
    - Haskell中心の記述を技術非依存の共通契約へ変更
    - 共通エラーの意味と分類
    - 構造化ログeventの共通項目
    - `execution_id`などの相関規則
    - execution／service／diagnostic eventの区別
    - 機密情報の扱い
    - loggerのencode・sink失敗時の意味
    - コンポーネント固有実装との責務境界
    - ADR-0001からADR-0002への現行参照切り替え
2. JSON Schemaの作成

構造化ログのserialized shapeを機械的に検証する正本です。配置場所やファイル名は、既存構成を確認したうえで設計書内に定義します。

3. fixtureの作成
    - Schemaへ適合する例
    - Schemaへ適合しない例
    - 後続のRS-0015とRS-0018が共通して利用する代表的なテスト入力

一方、RS-0014では次は行いません。

- Haskellの型やlogger実装
- Pythonの例外変換やlogger実装
- HTTP headerやrequest fieldの具体的な決定
- Embedding固有のerror codeや例外mapping
- retry executorの実装

したがって、最初はRS-0014を`in_progress`へ変更し、既存の[エラー・ログ設計.md](/Users/suzukitakashi/Desktop/RAGScope/RAGScope/docs/design/エラー・ログ設計.md)を精査して、改訂方針とSchema・fixtureの配置を決めるところから始まります。