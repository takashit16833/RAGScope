## 状況確認

Phase 0の内容自体は、ローカルでは完了しています。

|Phase 0項目|現在状態|判定|
|---|---|---|
|RS-0011を`planned`へ戻す|[`status: planned` (line 3)](/Users/suzukitakashi/Desktop/RAGScope/RAGScope/project-management/milestones/v0.0/document-ingestion/RS-0011 固定Markdown文書の取り込みとチャンク化を設計する.md:3)|完了|
|文書取り込みEpicを`planned`へ戻す|[`status: planned` (line 3)](/Users/suzukitakashi/Desktop/RAGScope/RAGScope/project-management/milestones/v0.0/document-ingestion/v0.0 固定Markdown文書の取り込みとチャンク化.md:3)|完了|
|`文書処理設計.md`を保持|[実在 (line 1)](/Users/suzukitakashi/Desktop/RAGScope/RAGScope/docs/design/文書処理設計.md:1)|完了|
|旧`エラー・ログ設計.md`の復帰可否確認|復帰済み|完了|
|復帰可能なら`docs/design/`へ戻す|[実在 (line 1)](/Users/suzukitakashi/Desktop/RAGScope/RAGScope/docs/design/エラー・ログ設計.md:1)|完了|
|復帰不能時だけ新規作成|該当なし|不要|

RS-0011とEpicのステージ済み差分は、それぞれ `in_progress → planned` の1行だけです。本文やFrontmatterの他項目は変更されていません。

## 現在のGit状態

現在は次の5ファイルがすべてステージされています。

```
A  AGENTS.md
A  docs/design/エラー・ログ設計.md
M  RS-0011 ...
M  文書取り込みEpic
A  無題のファイル.md
```

ここにはPhase 0以外のファイルも混ざっています。

- `AGENTS.md`：Google Drive運用からローカル正本運用への変更
- `無題のファイル.md`：過去のChatGPTレビューを保存した作業メモ
- 残り3件：Phase 0に直接関係する変更

したがって、Phase 0の作業内容は完成していますが、現状のステージ内容をそのまま一括commitするかは確認が必要です。

特に `無題のファイル.md` は、古いDrive情報、古いstatus、チャット形式の判断を含むため、現在仕様の正本にはできません。公開リポジトリへそのままcommitすることは推奨しません。

`AGENTS.md` もAI向け内部指示なので、公開リポジトリへ含めるか、ローカル専用にするかを決める必要があります。

## この後の推奨手順

### 1. Phase 0の変更単位を整理する

まず、Phase 0のcheckpointへ何を含めるか決めます。

Phase 0として自然なのは次の3件です。

- `docs/design/エラー・ログ設計.md`の復帰
- RS-0011の`planned`化
- 文書取り込みEpicの`planned`化

別扱いが自然なのは次です。

- `AGENTS.md`：Codex運用設定
- `無題のファイル.md`：内部ブレストメモ

まだcommitやunstageは実施していません。

### 2. Phase 0をcheckpointとして確定する

変更単位を整理した後、Phase 0を独立したcommitとして残すことを推奨します。

理由は、次のPhase 1で `エラー・ログ設計.md`、ADR、Milestone、Epic、Ticketが大きく変わるためです。復帰した旧設計を基準点として残せば、「復帰」と「再設計」の差分を分離できます。

### 3. Phase 1の変更対象を先に確定する

Phase 1で更新する中心文書は次です。

- 新規ADR-0002
- ADR-0001
- システムアーキテクチャ
- v0.0 Milestone
- 共通エラー・ログEpic
- RS-0014
- RS-0015
- 新規RS-0018
- 現行判断としてADR-0001を参照している文書

この段階では以下を行いません。

- RS-0014を`in_progress`にする
- `エラー・ログ設計.md`をHaskell・Python共通設計へ書き換える
- JSON Schemaやfixtureを作る
- コードを実装する
- RS-0001・RS-0002の機能固有eventを確定する

### 4. Phase 1を一貫した変更として反映する

適用順は次が安全です。

```
ADR-0002
↓
ADR-0001をsuperseded
↓
システムアーキテクチャ
↓
v0.0 Milestone
↓
共通エラー・ログEpic
↓
RS-0014
├─ RS-0015
└─ RS-0018
↓
関連文書のADR参照更新
```

ADR-0002には、今回採用済みの判断を集約します。

- Haskell・Pythonで契約を共有する
- 言語ごとの実装は兄弟Ticketに分ける
- JSON Schemaをserialized shapeの正本とする
- fixtureは代表的な検証入力とする
- `execution_id`はCLI command単位
- execution／service／diagnostic eventを区別する
- HTTP実伝播は後続Ticketへ分離
- retry／timeoutの既存原則を維持する
- PostgreSQL native logは対象外

### 5. Phase 2でレビューする

Phase 1反映後は、主に次を確認します。

- ADR-0002だけで現在判断を理解できるか
- ADR-0001の残すべき歴史的参照まで誤って置換していないか
- RS-0014、RS-0015、RS-0018の責務が重複していないか
- RS-0018へHTTP adapterやEmbedding固有処理が混入していないか
- RS-0011再着手の前提が明確か
- Frontmatter、相対リンク、Basesの所属が正しいか

このレビューが通るまでは、RS-0014は `planned` のままです。

## 直近の次アクション

直近では、Phase 1へ進む前に、現在ステージされている `AGENTS.md` と `無題のファイル.md` をPhase 0のcheckpointへ含めるか分離するかを決めるのが先です。

それが整理できれば、Phase 0は完了として確定し、次は「Phase 1の文書更新内容を実ファイルへ反映する」段階です。

今回もファイル変更、unstage、commitなどは行っていません。