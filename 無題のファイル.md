次は、**49件を一つずつその場で設計判断するのではなく、先に保全方針を1つ決めてから、適用時期ごとに処理する**のがよいです。

現在の台帳は、`要移行`2件、`要判断`49件です。旧設計書は、これらを処理して保存先を確定するまで削除できません。

## 推奨する進め方

### 第2段階：現在採用済みの2件を先に閉じる

`要移行`は次の2件です。

#### D-005 名称と命名規則

現在アップロードされている`RAGScope概要.md`には、すでに次が記載されています。

- `RAGScope`
    
- `rag-scope`
    
- `ragscope`
    
- `RAGSCOPE_`
    

そのため、実際のリポジトリへこの版を反映できているなら、D-005は追加の議論なしで`移行済み`へ変更できます。

#### D-009 RAGScope内の検索用語

- `retrieval`
    
- 全文検索
    
- dense検索
    
- hybrid検索
    
- reranking
    
- context
    
- stage
    

これらは現在の文書でも使われていますが、定義が一か所にまとまっていません。

これは現在すでに使っている用語なので、将来案ではありません。`システムアーキテクチャ.md`へ短い「用語」節として移すのが自然です。将来の`検索設計.md`では、距離尺度や順位付けなど検索固有の詳細だけを扱います。

この2件を処理すると、残るのは基本的に**採否が未決定の将来設計候補**になります。

---

## 第3段階：将来案の保存方法を決める

ここで一度だけ、ユーザー判断が必要です。

私の推奨は、非公開の次の文書を作ることです。

```text
RAGScope将来設計サルベージ.md
```

これはBacklogではなく、**旧設計書から救出した未採用の設計候補を保存するアーカイブ**です。

次の性質にします。

- 公開リポジトリへ置かない
    
- Ticket、期限、優先度、進捗状態を持たない
    
- 「やること一覧」にはしない
    
- 旧設計書の原文・図・数式を、必要な範囲で保持する
    
- 関連するMilestoneを記載する
    
- Milestone具体化時に、採用・変更・破棄を改めて判断する
    
- 移行台帳とは別に残す
    

たとえば次の構成です。

```markdown
# RAGScope将来設計サルベージ

## v0.1で再確認する候補

### 文書正規化とoffset
### Chunk・Evidence・Embeddingの対応
### 評価データ
### 検索評価指標
### Config・Run・Entity

## v0.1.5で再確認する候補

### AWSデプロイスパイク
### ネットワークとセキュリティ
### Stack・Secrets・削除・料金

## v0.2以降で再確認する候補

### no-answer
### 回答と引用
### 回答評価

## 非公開の実行環境条件

### 現在の開発マシン
```

これなら、旧設計書を丸ごと恒久保存する必要がなくなり、将来案も失いません。

## 第4段階：49件を適用時期ごとに一括処理する

49件をID順に議論するのは非効率です。台帳にはすでに`適用時期`があるため、次のまとまりで処理します。

### グループA：現在・全体

今決める必要があるものです。

例：

- `open-weight model`と`self-hosted model`の区別
    
- 回答が引用したsourceと評価上の根拠sourceを区別するか
    
- 引用完全性を正式な評価対象とするか
    

このグループだけは、一件ずつ私から推奨案と影響を提示し、ユーザーと決めます。

### グループB：v0.1

原則としてサルベージ文書へ保全し、v0.1具体化時に決めます。

- LF・NFC正規化
    
- Unicodeコードポイントoffset
    
- `Chunk.content`の不変条件
    
- EvidenceSpan
    
- Coverage／Density
    
- 代表evidence
    
- dev／test運用
    
- Config、Run、Entity、ER図
    
- canonical JSONとhash
    
- 実行環境の記録項目
    

v0.0では、正規化、offset、EvidenceSpan、評価基盤が明示的に対象外です。今これらを現在設計として確定すると、着手するMilestoneだけを具体化する規約と衝突します。

### グループC：v0.1.5

AWS関連をまとめて保全します。

- AWSデプロイスパイクの範囲
    
- Runtime Stack／Artifact Stack
    
- EC2、RDS、S3
    
- VPC、Security Group、Session Manager
    
- Secrets Manager
    
- CloudWatch
    
- 削除、snapshot、料金確認
    

Roadmapでは、AWSはRAG本体と分離した将来の検証として定義されています。詳細構成は、v0.1.5具体化時に見直すのが規約どおりです。

### グループD：v0.2以降

- no-answerの実現方式
    
- promptによる拒否
    
- 構造化引用の具体形式
    
- 回答・引用評価
    
- Ragas
    
- generation model関連
    

これらも対応Milestoneまでサルベージします。

### グループE：破棄または個人情報として保全

- `easy`・`terminology`タグ案
    
- 評価質問数の具体的目安
    
- M2 16GBという現在の開発環境
    
- 実装時に改めて決めるCLI例・JSON例
    

ここは、公開設計へ移すもの、非公開コンテキストへ置くもの、破棄するものに分けます。

---

## 第5段階：サルベージ文書と台帳を更新する

各項目の最終処理を次のいずれかにします。

```text
現在の正本へ移行
非公開サルベージへ保全
現行規約で置換
実装時確定
非公開コンテキストへ移行
破棄
```

非公開保全を選んだ項目は、単に台帳で`非公開保全`へ変えるだけでは完了ではありません。台帳以外の保存先へ実際に内容を移して、保存先を台帳に記録します。

## 第6段階：最終確認と削除

最終的には次の状態にします。

- `要移行`：0件
    
- `要判断`：0件
    
- 非公開保全項目：すべてサルベージ文書に保存済み
    
- `移行済み`：確認先と完了根拠あり
    
- `移行不要`：削除理由あり
    

その後、

1. もう一度別AIに最終レビューを依頼
    
2. 旧`ragscope-design.md`を削除
    
3. 移行台帳を削除
    
4. プロジェクトコンテキストの「移行期間」節を削除
    

と進めます。

## 直近の一手

次は、以下の順序が安全です。

```text
D-005を移行済みにする
    ↓
D-009をシステムアーキテクチャへ移す
    ↓
非公開サルベージ文書の形式を決める
    ↓
49件を適用時期別に処理する
```

私としては、まず**D-005とD-009を処理し、その後に非公開サルベージ文書の初版を作る**進め方を推します。