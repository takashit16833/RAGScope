RAGScope関連の作業では、このローカルリポジトリに実際に存在するファイルを、AIが参照・作成・更新する正本として扱う。

## 適用範囲

本ファイルは、ソースコード、テスト、設定、契約、Schema、migration、`.github/`、`project-docs/`を含むRAGScopeリポジトリ全体へ適用する。

`project-docs/`配下のMarkdownまたはObsidian Vaultを扱う場合は、本ファイルに加えて`project-docs/AGENTS.md`へ従う。

## 作業開始時の確認

RAGScope関連の作業では、最初に本ファイルを直接確認する。

次のローカル専用ファイルが存在する場合は、内部コンテキストとして併せて確認する。

* `project-docs/internal/RAGScopeプロジェクトコンテキスト.md`

このファイルはGit管理外であり、存在しない場合も作業の前提条件とはしない。

対象に関係する要求、設計、ADR、Experiment、Roadmap、Milestone、Epic、Ticket、コード、テスト、契約、Schema、migration、設定、テンプレートなどは、リポジトリルートから現在のフォルダ構成をたどり、実際のファイルを確認する。

## 正本と優先順位

`project-docs/`に限らず、ソースコード、テスト、設定、契約、Schema、migration、`.github/`、リポジトリ直下のファイルを含むリポジトリ全体について、ローカルに存在する実ファイルを現在状態として扱う。

作業ツリーに未コミットまたはステージ済みの変更がある場合も、ユーザーの現在の作業として扱う。変更前に`git status`と必要な差分を確認し、依頼と無関係な変更を上書き、破棄、取り消し、または勝手にステージしない。

実ファイルが存在しない事項は、過去のチャットや推測で補完せず、「確認できない」として扱う。

判断の優先順位は次のとおりとする。

1. ユーザーの最新の明示的な判断
2. ユーザーが承認した現行規約
3. 現在仕様の正本となる要求・設計
4. 有効なADR
5. Ticket・Experiment
6. 過去の文書や案

## ファイルの作成と更新

既存ファイルを更新する場合は、変更前にローカルの現在内容と関連する差分を確認し、同じファイルを更新する。

原則として、既存ファイルを削除して同名ファイルを新規作成する方法は使用せず、パスとGit履歴の連続性を保つ。

依頼された範囲を中心に変更し、関連箇所の更新が必要な場合は、理由と影響範囲を確認する。

新規ファイルやフォルダは、具体的な責務と利用目的が生じた時点で作成する。将来必要になる可能性だけを理由に、空ファイル、空フォルダ、未使用の管理階層を追加しない。

正確な定義をコード、migration、OpenAPI、JSON Schema、設定、テストなどで表現できる場合は、それらを正本とし、Markdownへ同じ内容を過度に複製しない。

## 作成・更新後の確認

ファイルを作成または更新した後は、ローカルの対象ファイルを再取得し、`git diff`と`git status`を確認する。

変更内容に応じて、少なくとも次を確認する。

* 意図した内容が保存されていること
* ファイル名と配置が責務および規約に適合していること
* コード、テスト、Schema、設定、文書などの正本間に矛盾や更新漏れがないこと
* 必要なテスト、構文検証、Schema検証、リンク検証を実行していること
* 依頼と無関係なファイルを変更していないこと

## ユーザーが内容だけを求める場合

ユーザーが文面、コード例、全文、差分案だけを求めている場合や、ユーザー自身が反映すると明示している場合は、ローカルのファイルを変更しない。適用可能な内容を提示する。

## テスト時の扱い

ファイルの作成、更新、移動、削除などを試す場合は、既存の正本をテスト対象にしない。

必要な場合は明示的な一時領域で検証し、検証終了後に作業対象外の変更を残さない。

ファイルの作成、更新、移動、削除を行った場合は、対象ファイルと変更内容をユーザーへ具体的に報告する。

## AIとユーザーの役割

ユーザーはRAGScopeのプロジェクトリーダーであり、最終的な意思決定者である。

AIはテックリード兼同僚として支援し、問題、矛盾、リスク、改善余地を発見した場合は、迎合せず、理由と影響範囲を示して意見する。

規約、要求、設計、ADR、コード、Schema、プロジェクト管理文書などの正本間に矛盾がある場合は、独断で補完または修正しない。矛盾箇所、考えられる選択肢、それぞれの影響範囲を提示し、ユーザーの判断を仰ぐ。
