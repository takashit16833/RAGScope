



確認できました。**Google Drive上のRAGScope Vaultを直接たどり、RS-0011で行う作業を把握しました。** 今回は確認のみで、ファイルの作成・更新は行っていません。

## 現在の位置づけ

現在進行中なのは、Milestone **v0.0「最小のdense検索を実行できる」**です。固定Markdown文書を読み込み、チャンク化、Embedding生成・保存、質問によるexact vector searchまでをローカルで通す最小構成になっています。fileciteturn12file0L6-L25

その最初のEpicが、**「固定Markdown文書の取り込みとチャンク化」**です。ここでは、入力文書をHaskellで読み込み、後続のEmbedding生成へ渡せるチャンク列へ変換します。文書version、正規化済み本文、厳密なoffset管理などは、このEpicでは扱いません。fileciteturn11file0L6-L26

Ticketの状態は次の順序です。

```text
RS-0011 設計                           in_progress
   ↓
RS-0001 固定Markdownファイルの読み込み planned
   ↓
RS-0002 固定ルールによるチャンク化     planned
```

RS-0001とRS-0002は、どちらもRS-0011の完了と`docs/design/文書処理設計.md`の存在を前提にしています。fileciteturn18file0L20-L24 fileciteturn19file0L20-L24

## RS-0011で作るもの

新規に次のファイルを作成します。

```text
docs/design/文書処理設計.md
```

現在の`docs/design/`には`システムアーキテクチャ.md`しかなく、`文書処理設計.md`はまだ存在しません。fileciteturn14file0L3-L22

Frontmatterは、規約に従ってこれだけです。

```yaml
---
note_type: design
---
```

設計書には`status`などを追加しません。メタデータ規約では、設計書の`note_type`は`design`で、`status`はADR・Milestone・Epic・Ticketだけに使用すると定められています。fileciteturn20file0L38-L67 fileciteturn20file0L124-L132

## 設計で確定させる内容

RS-0011では、次の設計を文章化します。

### 文書読み込み

- UTF-8のMarkdownファイル1件を対象とする
- 実行時の入力パスはv0.0では固定する
- 読み込み処理自体はテスト可能な境界として分離する
- ファイル全体をHaskellの`Text`として扱う
- Markdown記法、見出し、Frontmatter、空行、改行をそのまま保持する
- 正規化、前後空白の削除、Markdown解析は行わない
- 存在しないファイルは正常系と区別できる失敗にする

正確な関数名、module名、型名、libraryまでは設計書で固定せず、実装コードを正本にします。fileciteturn10file0L22-L34 fileciteturn18file0L25-L36

### チャンク化

- 先頭から**1,000 Unicodeコードポイント単位**
- overlapなし
- 最後のチャンクだけ1,000未満を許容
- 各チャンクは、元文書を識別する概念、0始まりの連続した`chunkIndex`、本文を持つ
- チャンク列は後続のEmbedding生成から利用できる値として返す
- チャンク本文を`chunkIndex`順に連結すると、入力本文と完全一致する

つまり、文字の削除、重複、置換、並べ替えを許さないことが主要な不変条件です。fileciteturn19file0L25-L37 fileciteturn19file0L61-L70

### エラーと境界条件

少なくとも次を設計上明示します。

- ファイルが存在しない場合
- 空本文の場合
- 1,000コードポイント未満
- ちょうど1,000コードポイント
- 1,000を超える場合
- 読み込みまたはチャンク化に失敗した値を、後続のEmbedding生成へ渡さないこと

特に空本文は有効な検索対象とせず、**0件のチャンクとして正常扱いするか、文書処理エラーとして扱うか**を設計書で明確にする必要があります。Ticketでは結論までは固定されておらず、「文書処理設計で定めた結果を返す」とされています。fileciteturn10file0L29-L34 fileciteturn19file0L63-L69

## v0.0と将来設計の境界

ここが今回いちばん重要です。

システム全体のアーキテクチャでは、将来的な文書取り込みを次の流れとして定義しています。

```text
文書
→ 正規化
→ 正規化済み本文を保存
→ 条件に従って分割
→ 文書断片と位置を保存
→ Embedding生成
```

fileciteturn16file0L177-L186

一方、v0.0では正規化、offset、文書version、可変チャンク条件を明示的に対象外としています。fileciteturn12file0L27-L37

したがって、`文書処理設計.md`では次のように分けるのが自然です。

```text
現在採用するv0.0の設計
- 生のMarkdown本文をそのまま読み込む
- 固定1,000コードポイントで分割する
- 永続化や厳密な追跡は行わない

将来拡張との境界
- 正規化
- offset
- 文書version
- ChunkSet / 分割条件管理
- Markdown構造を利用した分割
```

これにより、システムアーキテクチャを否定せず、v0.0がその一部を段階的に実現する設計だと明確にできます。要求についても、v0.0が直接扱うのは主に`REQ-DOC-001`と`REQ-DOC-002`で、厳密な追跡や条件管理を求める`REQ-DOC-003`以降は後続段階です。fileciteturn15file0L23-L31 fileciteturn12file0L39-L51

## RS-0011完了時の変更対象

RS-0011で変更する対象は、基本的に次の3点です。

1. `docs/design/文書処理設計.md`を新規作成する  
2. 所属Epicの「関連文書」に`文書処理設計.md`へのリンクを追加する  
3. RS-0011の完了条件を確認し、`結果`を記入して`status: done`へ変更する  

設計書には責務、契約、不変条件、処理フロー、境界条件を記載し、正確な型や具体的なテストケースはコード・テストを正本とします。fileciteturn21file0L64-L77 fileciteturn10file0L22-L36

作成後はGoogle Driveから再取得し、Frontmatter、配置、相対リンク、要求・アーキテクチャ・Epic・後続Ticketとの整合を確認する必要があります。

**RS-0011は、実装を始めるTicketではなく、RS-0001とRS-0002が迷わず実装できるだけの初期設計を完成させるTicket**、という理解です。次は、この内容に基づいて`文書処理設計.md`の作成へ進める状態です。