---
note_type: milestone
status: in_progress
---
# v0.0 — 最小のdense検索を実行できる

## 目標

少量の固定Markdown文書を取り込み、固定ルールで文書チャンクへ分割して、文書チャンクのEmbeddingを保存する。RAGScope CLIから質問を入力し、近似インデックスを使わないベクトル検索（exact vector search）で質問に近い文書チャンクを取得・表示できる、最小のdense検索経路をローカル環境で動作させる。

あわせて、共通エラー・構造化ログ契約と、契約を利用する各コンポーネントの最小基盤を整える。再試行・タイムアウトは共通原則を定め、具体的な外部処理に必要な範囲だけ導入する。

## 対象範囲

### dense検索の最小経路

- 少量の固定Markdown文書の読み込み
- RAGScopeアプリケーションによる固定ルールでの文書チャンク分割
- AI推論サービスによる文書チャンクのEmbeddingと質問Embeddingの生成
- PostgreSQL / pgvectorへの文書チャンクとEmbeddingの保存
- RAGScope CLIからの質問入力
- pgvectorを使用した、近似インデックスを使わないベクトル検索（exact vector search）
- 検索結果となる上位の文書チャンクのRAGScope CLIへの表示
- ローカル環境での文書取り込みから検索結果表示までの一連の実行

### 共通エラー・構造化ログ

- v0.0の各コンポーネントで共通利用するエラー表現と構造化ログの最小契約
- 構造化ログをJSONへ変換したときの項目構成を定義するJSON Schemaと、各実装で共有する代表的な検証用データ（fixture）
- 契約を利用する各コンポーネントの構造化ログ基盤
- 正常なCLI出力・HTTP応答から分離した、ローカル環境向けのログ出力

### 再試行・タイムアウト

- 再試行・タイムアウトを、一時的な失敗が発生し、かつ同じ処理を繰り返しても安全な場合に限定する共通原則
- 具体的な利用箇所がない汎用的な実行機構を先行実装しない方針
- RAGScopeアプリケーションからAI推論サービスへ文書チャンクのEmbeddingを要求する処理種別（`operation`）に必要な、再試行・タイムアウトの機能別設計と最小実装

## 対象外

- TXT、PDF、画像など、固定Markdown以外の文書形式への対応
- 文書、文書集合、チャンク条件、Embedding条件のバージョン管理
- 正規化規則、位置（`offset`）、`EvidenceSpan`による元文書への厳密な追跡
- 評価データ、検索指標、実験結果の保存・比較、Markdown / CSV形式のレポート
- PostgreSQL全文検索、hybrid検索、`reranking`
- 回答生成、構造化引用、回答不能（`no-answer`）判定
- HNSW / IVFFlatなどの近似検索インデックス
- AWSへの配置、CloudWatchへのログ収集、保持期間、アラームなどのAWS固有運用
- 本格的な分散追跡（tracing）、性能集計
- 本格的なWeb UI、認証、ストリーミング、バッチ処理、非同期ジョブ
- v0.5以降で決定する分散追跡の正確な相関規則と、バッチ処理・非同期ジョブ固有の再実行・重複防止方式

## 対象要求

> [!note] 要求との対応
> 要求の全文と確認方法は[RAGScope要求定義](../../../RAGScope要求定義.md)を正本とする。
> ここでは、このMilestoneが各要求のどこまでを担当するかだけを記載する。

| 要求ID | このMilestoneで実現する範囲 |
|---|---|
| [`REQ-DOC-001`](<../../../RAGScope要求定義.md#2.1 文書の取り込みと追跡>) | 固定パスのMarkdown文書1件を読み込み、後続処理で利用できる本文として取り込む。 |
| [`REQ-DOC-002`](<../../../RAGScope要求定義.md#2.1 文書の取り込みと追跡>) | 取り込んだMarkdown本文を固定ルールで複数の文書チャンクへ分割する。 |
| [`REQ-RET-002`](<../../../RAGScope要求定義.md#2.2 検索>) | 文書チャンクのEmbeddingと質問Embeddingを生成し、pgvectorの近似インデックスを使わないベクトル検索（exact vector search）によって、質問に近い文書チャンクを順位付きで取得する。 |
| [`REQ-USE-001`](<../../../RAGScope要求定義.md#2.7 実行とレポート>) | RAGScope CLIから質問を入力してdense検索結果を表示し、文書取り込みから検索結果表示までの手順をローカル環境で再実行できるようにする。 |
| [`REQ-NF-REL-001`](<../../../RAGScope要求定義.md#3.3 信頼性と保守性>) | v0.0で追加する文書読み込み、文書チャンク分割、Embedding生成・保存、dense検索の主要な正常系と異常系を、自動テスト、結合テスト、一連の動作確認（end-to-end）によって確認する。 |
| [`REQ-NF-REL-002`](<../../../RAGScope要求定義.md#3.3 信頼性と保守性>) | AI推論サービス、モデル処理、PostgreSQL処理の失敗を、正常結果と区別できる処理状態とエラーとして扱う。 |
| [`REQ-NF-REL-003`](<../../../RAGScope要求定義.md#3.3 信頼性と保守性>) | RAGScopeアプリケーションからAI推論サービスへ文書チャンクのEmbeddingを要求する処理種別（`operation`）についてタイムアウトを扱い、一時的な失敗であり、かつ同じ処理を繰り返しても安全な場合に必要な範囲で再試行を適用する。 |
| [`REQ-NF-REL-005`](<../../../RAGScope要求定義.md#3.3 信頼性と保守性>) | ローカル実行の処理進行と失敗をログとして出力し、一連の実行に関係するログを識別して確認できるようにする。実験識別子は実験管理を導入するMilestoneで追加する。 |

## 成功条件

### dense検索の最小経路

- [ ] RAGScopeアプリケーションで固定Markdown文書を読み込み、本文を後続処理へ渡せる
- [ ] 読み込んだ文書を固定ルールで文書チャンクへ分割できる
- [ ] AI推論サービスで文書チャンクのEmbeddingを生成できる
- [ ] 文書チャンクとEmbeddingをPostgreSQL / pgvectorへ保存できる
- [ ] RAGScope CLIから質問を入力し、質問Embeddingを生成できる
- [ ] pgvectorの近似インデックスを使わないベクトル検索（exact vector search）で、質問に近い上位の文書チャンクを取得できる
- [ ] 取得した上位の文書チャンクをRAGScope CLIへ表示できる
- [ ] 文書取り込みから検索結果表示までを、定めた手順でローカル環境から再実行できる

### 共通エラー・構造化ログ

- [ ] 共通エラー契約と構造化ログの設計が定義され、JSON Schemaと代表的な検証用データ（fixture）から契約を確認できる
- [ ] 契約を利用する各コンポーネントで、それぞれの最小基盤を利用できる
- [ ] ローカルでは正常なCLI出力・HTTP応答とログが分離され、1回のコマンド実行内でコンポーネント境界を越えて引き継いだ`execution_id`により、同じ実行に関係する構造化ログを識別して確認できる
- [ ] アプリケーションのログイベントと、アプリケーションが観測したPostgreSQL処理の開始・成功・失敗を共通形式で確認できる

### 再試行・タイムアウト

- [ ] 再試行・タイムアウトの共通原則がADRで確定し、各処理種別（`operation`）について自動再試行の有無と理由を判断できる
- [ ] RAGScopeアプリケーションからAI推論サービスへ文書チャンクのEmbeddingを要求する処理種別について、必要な再試行・タイムアウトが機能別設計に基づいて実装・テストされる
- [ ] 各機能の設計で、発生するエラー、出力するログ、自動再試行の有無を説明できる

## 関連文書

- [ADR-0002 — 共通実行基盤の契約とコンポーネント実装を分離する](<../../../adr/ADR-0002 共通実行基盤の契約とコンポーネント実装を分離する.md>)

## Epic別Ticket

```base
filters:
  and:
    - 'file.inFolder("project-management/milestones")'
    - 'note_type == "ticket"'
    - 'milestone == this'
views:
  - type: table
    name: すべて
    groupBy:
      property: epic
      direction: ASC
    order:
      - file.name
      - epic
      - status
  - type: table
    name: 未完了
    filters:
      or:
        - 'status == "planned"'
        - 'status == "in_progress"'
    groupBy:
      property: epic
      direction: ASC
    order:
      - file.name
      - epic
      - status
```

## リリース結果

> [!note] 完了時に記入
> - 実現した内容
> - 実行・確認方法
> - 既知の制約
> - Git tag `v0.0`を付与したコミット
