---
note_type: ticket
status: in_progress
milestone: "[[v0.0]]"
epic: "[[v0.0 共通エラーと構造化ログによる実行追跡]]"
---
# RS-0014 共通エラーと構造化ログを設計する

## 目的

v0.0では、文書読み込み、AI推論サービスとの通信、PostgreSQL処理など、異なる種類の処理と失敗を扱う。各実装Ticketが独自のエラー型、エラーコード、ログ項目、相関情報を決めると、処理全体を追跡できず、後から共通化する際に広範囲な修正が必要になる。

このTicketでは、後続の共通基盤実装と機能実装に先立ち、現在Haskell中心で記載されている`design/エラー・ログ設計.md`を、複数のコンポーネントが利用できる技術非依存の契約へ改訂する。構造化ログのserialized shapeをJSON Schemaとして定義し、各実装で共有する代表的なfixtureを作成する。v0.0のローカル実行で必要な最小範囲を定義し、将来のHTTP、AWS収集、tracingへ拡張できる境界を保ちつつ、現時点で使用しない詳細は固定しない。

## 前提

- [ADR-0002 — 共通実行基盤の契約とコンポーネント実装を分離する](<../../../../adr/ADR-0002 共通実行基盤の契約とコンポーネント実装を分離する.md>)が`accepted`である
- [v0.0 — 最小のdense検索を実行できる](../v0.0.md)に、共通エラーと構造化ログの最小基盤が対象として記載されている

## 完了条件

- [ ] `design/エラー・ログ設計.md`が、複数のコンポーネントで利用できる現在設計を扱う`note_type: design`の機能設計書へ改訂されている
- [ ] 共通する意味とserialized shape、およびコンポーネントごとに実装する型、例外変換、encoder、logger、sinkの責務境界が記載されている
- [ ] 共通エラー、CLIなどの利用者向けエラー表示、HTTPなどの境界で受け渡す失敗、構造化ログイベントの責務と変換境界が区別されている
- [ ] エラーの分類、安定したエラーコードの命名、利用者向けmessage、内部原因、追加contextの扱いが定義されている
- [ ] エラー分類とログlevelを別の概念として扱い、v0.0で使用するログlevelと意味が定義されている
- [ ] 構造化ログを、共通Envelope、実行Context、イベント固有Payloadに分ける方針が記載されている
- [ ] v0.0で必要な共通項目、event名の命名規則、event内の`schema_version`、Schemaファイル自体のversion、時刻、level、component、operation、error codeの扱いが定義されている
- [ ] 使用するJSON Schema draft、timestampの形式とtimezone、IDの文字列表現が定義されている
- [ ] 構造化ログのserialized shapeを機械的に検証できるJSON Schemaが、設計で定めた配置に作成されている
- [ ] JSON Schemaが共通field・共通category・variant・error codeの形式を扱い、機能固有のerror codeを巨大な共通`enum`として正本化していない
- [ ] Schema適合・不適合の代表的なfixtureが、設計で定めた配置に作成されている
- [ ] fixtureが仕様正本ではなく代表的な検証入力であることと、契約を利用する各実装から同じfixtureを利用する方針が記載されている
- [ ] JSON field順に依存せず、意味とSchema適合性を確認するテスト方針が定義されている
- [ ] 1イベントを識別する情報と、1回のCLI commandに属するイベントを識別する`execution_id`の生成・引き継ぎ規則が定義されている
- [ ] 文書取り込みcommandと検索commandは別の`execution_id`でよく、各command内ではコンポーネント境界を越えて同じ`execution_id`を伝播する方針が定義されている
- [ ] execution-scoped event、service-scoped event、health・readinessなどのdiagnostic request eventの境界と、`execution_id`の要否が定義されている
- [ ] 該当しないoptional項目を無制限な`null`として出力せず、イベントごとの構造を保つ方針が記載されている
- [ ] ローカルでは正常出力をstdout、構造化ログをstderrへ分離し、1イベント1行のJSON Linesとして出力する方針が記載されている
- [ ] アプリケーションイベントの意味と具体的なsinkを分離し、stderr向けsinkとテスト用sinkを利用できる設計になっている
- [ ] encode失敗、sink失敗、fallback出力の採否と形式、fallback自身の失敗を含むlogger失敗時の意味が定義されている
- [ ] 元のoperation失敗をlogger失敗で上書きしないことと、失敗したsinkへ再帰的にeventを書き込まないことが定義されている
- [ ] failing sinkを用いて、各実装で同じ失敗時の意味を確認するテスト方針が記載されている
- [ ] 文書本文、prompt、secret、認証情報などを不用意にログへ出さないための記録・masking方針が記載されている
- [ ] コンポーネント間で失敗情報を受け渡す際に共有する意味、runtime・library例外を安全な共通エラーへ変換する境界、正確なHTTP schemaをAPI設計・OpenAPIで具体化する境界が記載されている
- [ ] retry / timeoutは別の実行制御であり、共通エラーへoperation横断の単純な`retryable`判定を埋め込まない方針が記載されている
- [ ] CloudWatchの具体設定、tracingの正確な相関規則、batch・非同期job固有の情報は後続Milestoneで決定することが明記されている
- [ ] 正確な型、例外変換、encoder、logger、sinkは各コンポーネントのコード、具体的なテストケースはテストコードを正本とすることが明記されている
- [ ] 現行判断としてADR-0001を参照している記述がADR-0002へ切り替えられ、ADR-0001への参照は経緯を説明する歴史的参照に限定されている
- [ ] 所属Epicの`関連文書`から`エラー・ログ設計.md`を参照できる状態になっている
- [ ] 関連する要求定義、システムアーキテクチャ、ADR、v0.0 Milestoneに矛盾しないことを確認できる

## 対象外

- 各コンポーネントの共通エラー型、例外変換、logger、sink、JSON encoderの実装
- 文書処理、Embedding生成、DB処理など、各機能で発生する具体的なエラーとイベントの網羅
- HTTP APIの正確なrequest / responseとエラーschemaの確定
- retry回数、待機、backoff、jitter、timeoutなどoperation固有の実行制御
- CloudWatchのlog group、保持期間、metric filter、alarmの設計
- tracingの正確な`trace_id`、`span_id`、親子関係、処理間Linkの規則
- 実験結果と処理状態のPostgreSQLへの永続化

## 関連文書

- [RAGScope要求定義「3.3 信頼性と保守性」](<../../../../RAGScope要求定義.md#3.3 信頼性と保守性>)
- [システムアーキテクチャ「3.4 共通実行基盤の責務境界」](<../../../../design/システムアーキテクチャ.md#3.4 共通実行基盤の責務境界>)
- [システムアーキテクチャ「4. 依存方向」](<../../../../design/システムアーキテクチャ.md#4. 依存方向>)
- [ADR-0002 — 共通実行基盤の契約とコンポーネント実装を分離する](<../../../../adr/ADR-0002 共通実行基盤の契約とコンポーネント実装を分離する.md>)
- [v0.0 — 最小のdense検索を実行できる](../v0.0.md)

## 実装メモ

- 設計書には、実装だけでは分かりにくい責務、契約、不変条件、処理フロー、変更時の注意を記載する。
- 正確な型定義やJSON encoderのコードをMarkdownへ複製しない。
- v0.0ではCLI commandとservice lifecycleを具体的なContextとして扱い、HTTPやbatchの完全なschemaを先回りして作らない。
- 将来のtracingへ拡張できる意図は残すが、現時点で`trace_id`やSpan構造を採用済みとして記載しない。

## 結果

> [!note] 完了時に記入
> - 改訂したエラー・ログ設計の概要
> - 確定した共通契約とローカル出力方針
> - 作成したJSON Schemaとfixture
> - 後続の共通基盤実装Ticketへ渡した実装判断
> - 後続機能へ残した適用事項
> - 既知の未決定事項
> - 関連Pull Request
