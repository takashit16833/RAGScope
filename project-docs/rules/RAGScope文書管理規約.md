---
note_type: reference
---
# RAGScope文書管理規約

> [!abstract] この文書の役割
> RAGScopeにおける文書の配置、責務、正本、参照関係を定義する。  
> 「何をどこへ記載するか」を統一し、文書間の重複や矛盾を防ぐ。

## 1. 適用範囲

本規約は、RAGScopeリポジトリで人が作成・管理し、利用者または開発者が読む文書、および文書として管理する成果物へ適用する。

本規約で`rules/`、`design/`、`adr/`、`experiments/`、`project-management/`、`templates/`、Vault直下のMarkdownと記載する場合は、Obsidian Vaultである`project-docs/`を基準とする。`README.md`と`.github/`は、リポジトリルートを基準とする。

主な対象は次のとおりとする。ここに列挙されていない文書でも、同じ性質を持つものには本規約を適用する。

- リポジトリルートの`README.md`
- Vault直下の`RAGScope概要.md`、`RAGScope要求定義.md`、`RAGScope開発運用ガイド.md`
- `rules/`配下の各種規約文書
- `design/`配下のシステム・機能設計
- `adr/`配下の設計判断
- `experiments/`配下の実験記録
- `project-management/`配下のRoadmap、Milestone、Epic、Ticket
- `templates/`配下のObsidianテンプレート
- リポジトリルートの`.github/pull_request_template.md`

ソースコード、テストコード、migration、OpenAPIなどのAPI定義、設定ファイル、依存関係のlock file、CI/CD定義、ローカル開発環境・エディタ設定、ビルド生成物、一時ファイルには適用しない。ただし、それらの正確な定義とMarkdownの説明を重複させないための扱いは、第2.3節に従う。

`internal/`は任意のローカル専用領域としてGit管理から除外し、存在を必須としない。配下の非公開資料は公開文書の正本として使用せず、公開文書からリンクしない。内部資料の扱いはリポジトリルートの`AGENTS.md`に従う。

Frontmatterは[Obsidianメタデータ規約](./Obsidianメタデータ規約.md)、Roadmap・Milestone・Epic・Ticketの運用は[RAGScopeプロジェクト管理規約](./RAGScopeプロジェクト管理規約.md)に従う。

一般的なAI・LLM・RAGの学習ノートは、本リポジトリへ置かない。

## 2. 基本原則

### 2.1 1つの情報に1つの正本を設ける

| 情報 | 正本 |
|---|---|
| RAGScopeの目的、価値、想定利用方法、主な能力の概要 | `RAGScope概要.md` |
| 機能・品質・制約・対象範囲・対象外の詳細 | `RAGScope要求定義.md` |
| 現在採用しているシステム・機能の設計 | `design/` |
| 重要な設計判断の背景、選択肢、理由 | `adr/` |
| 仮説、検証条件、結果、考察 | `experiments/` |
| バージョン計画と作業の進行記録 | `project-management/` |
| 正確な実装、型、スキーマ、テスト | コード、migration、OpenAPI、テストなど |

同じ情報を複数の文書で正本として管理しない。別の文書で触れる場合は簡潔に要約し、詳細の正本へ相対リンクする。

`README.md`は入口と要約であり、アプリケーション概要の正本ではない。

### 2.2 現在仕様と履歴を分ける

- 要求定義と設計書は、現在有効な内容を記載する。
- ADRは、重要な判断の背景、選択肢、決定、影響を記録する。
- 実験記録は、実際に行った検証と得られた結果を記録する。
- Ticketは、個別の変更作業、完了条件、実施結果を記録する。
- Gitは、ファイルの変更履歴を保持する。

現在仕様を理解するために、過去のTicketやMilestoneを読むことを必須にしない。

### 2.3 機械可読な仕様を優先する

正確な定義をコードや専用形式で表現できる場合は、それを正本とする。

| 内容 | 正本 |
|---|---|
| DBのテーブル・カラム・制約 | migration |
| APIの正確なschema | OpenAPIなどのAPI定義 |
| Haskell・Pythonの型 | ソースコード |
| CLIの正確なオプション | 実装と`--help` |
| 具体的なテストケース | テストコード |
| パッケージバージョン | lock file・設定ファイル |

Markdownには、実装だけでは分かりにくい責務、理由、契約、不変条件、処理フロー、変更時の注意を記載する。機械可読な定義をそのまま複製しない。

### 2.4 必要な文書だけを作る

- 具体的な内容と利用目的が生じた時点で文書を作成する。将来必要になる可能性だけを理由に、空文書や空フォルダを作らない。
- 既存文書で責務を満たせる場合は新しい文書を作らず、1つの文書が異なる問いを扱うようになった場合に分割する。

## 3. 文書構成

```text
RAGScope/
├── README.md
├── .github/
│   └── pull_request_template.md
└── project-docs/
    ├── AGENTS.md
    ├── RAGScope概要.md
    ├── RAGScope要求定義.md
    ├── RAGScope開発運用ガイド.md
    ├── rules/
    │   ├── Obsidianメタデータ規約.md
    │   ├── RAGScope文書管理規約.md
    │   └── RAGScopeプロジェクト管理規約.md
    ├── design/
    │   ├── システムアーキテクチャ.md
    │   └── <必要になった機能設計>.md
    ├── adr/
    │   └── ADR-0001 <判断の題名>.md
    ├── experiments/
    │   └── <実施した検証>.md
    ├── project-management/
    │   ├── ロードマップ.md
    │   └── milestones/
    └── templates/
        ├── 設計書テンプレート.md
        ├── Milestoneテンプレート.md
        ├── Epicテンプレート.md
        └── Ticketテンプレート.md
```

- `experiments/`は、最初の内容が生じた時点で作成する。
- `internal/`はGit管理する標準構成に含めず、ローカル専用の資料が必要な場合だけ作成する。
- `RAGScope開発運用ガイド.md`は、プロジェクト管理規約に基づく具体的な操作の入口として使用する。
- `templates/`とリポジトリルートの`.github/pull_request_template.md`は、開発運用ガイドに従ってノート作成・PR作成に使用する。
- 機能別設計書は、技術要素ではなく設計対象となる機能・責務で分ける。
- Roadmap、Milestone、Epic、Ticketの詳細な構成は、RAGScopeプロジェクト管理規約を正本とする。

## 4. 文書ごとの責務

### 4.1 `README.md`

リポジトリの入口として、次を簡潔に記載する。

- RAGScopeの短い説明
- 主な能力
- 現在利用できる機能と最新リリースの概要
- 最小限のセットアップ・実行方法
- 簡略化した構成
- 代表的な生成結果
- 詳細文書への導線

RAGScopeの目的・価値・対象範囲は、`RAGScope概要.md`の内容を要約し、詳細は同文書へリンクする。

READMEには、作業中のMilestone名や一時的な`status`を手作業で転記しない。開発予定はRoadmapへリンクし、作業中のMilestoneはMilestoneノートの`status`とBases・検索から確認する。READMEの状態説明は、現在利用できる機能と最新リリースの内容を中心とする。

READMEへ詳細な設計、要求一覧、Ticket一覧、実験の生データを記載しない。

### 4.2 `RAGScope概要.md`

RAGScopeというアプリケーションの概要の正本とする。

- 解決する問題
- 提供する価値
- 想定する利用方法
- 主な能力
- 対象範囲の概要
- 対象外・主張しないことの概要

主な能力、対象範囲、対象外は高い粒度で要約し、詳細かつ検証可能な定義は`RAGScope要求定義.md`を正本とする。

詳細な機能要件、技術選定、設計、バージョン計画は記載しない。

### 4.3 `RAGScope要求定義.md`

RAGScopeが満たすべき内容の正本とする。

- 機能要件
- 非機能要件
- 制約
- 対象外
- 必要に応じて、要件の識別子と確認方法

要求定義には「何を満たすか」を記載する。「どのコンポーネント、型、DB、ライブラリで実現するか」は設計書へ記載する。

### 4.4 `design/`

`design/`全体をRAGScopeのシステム設計とする。

#### `システムアーキテクチャ.md`

システム全体に関する次の内容を記載する。

- コンポーネント構成
- 各コンポーネントの責務
- 依存方向
- コンポーネント間の通信
- システム全体の処理・データフロー
- 配置・実行環境の基本方針

#### 機能別設計書

機能別設計書は、対象機能について現在採用している設計の正本とする。新しい機能の作成または既存機能の変更により、完了後も有効な設計情報が生じる場合に、対象機能の設計書を作成または更新する。

例：

```text
文書処理設計.md
データモデル設計.md
検索設計.md
評価設計.md
API設計.md
```

Ticketごとに新しい設計書を作ることは必須としない。既存の機能設計書で責務を自然に扱える場合は、その設計書を更新する。異なる機能・責務を扱うようになった場合に、新しい設計書へ分ける。

各設計書は、過去のTicketや変更記録を読まなくても、現在採用している設計を理解できる状態にする。判断理由を詳しく残す必要がある場合は、ADRへリンクする。

### 4.5 機能設計の粒度と作業記録との分離

基本設計と詳細設計を、`basic-design/`と`detailed-design/`のような別フォルダや文書体系へ分けない。対象機能の同じ設計書に、責務、入出力、処理フロー、コンポーネント間の関係などの全体像から、不変条件、厳密な規則、エラー処理、境界条件などの詳細まで、必要な粒度を記載する。

機能設計書を新規作成する場合は、原則として[`templates/設計書テンプレート.md`](<../templates/設計書テンプレート.md>)を使用する。テンプレートの次の章は、すべての機能設計書に設ける。

1. `目的と適用範囲`
2. `関連する要求と上位設計`
3. `責務と境界`

第4章以降は、設計対象に必要な内容だけを残す。基本設計と詳細設計は同じ文書内で粒度を区別してよいが、小規模な設計で分ける意味がない場合は統合してよい。内容のない見出しや、形式を満たすためだけの「該当なし」は残さない。

今回の変更目的、作業範囲、完了条件、実施結果はEpic・Ticketへ記録し、機能設計書には完了後も有効な現在設計を記載する。重要な設計判断の理由はADR、実測や比較を伴う検証はExperimentへ記録する。

詳細がコード、migration、OpenAPI、テストなどの機械可読な正本と重複する場合は、正確な定義を複製せず、その設計意図や守るべき制約を記載する。

#### 読みやすい機能設計書の表現

機能設計書は、文章を最初から順番に読まなくても、見出しと視覚表現から全体像、情報の階層、必要な規則の場所を把握できる構成にする。

最初に全体像を示し、その後に詳細と補足を置く。異なる粒度や役割の情報を同じ見た目の文章として連続させず、内容に応じて次の表現を使い分ける。

| 内容 | 優先する表現 |
|---|---|
| 処理の順序、分岐、コンポーネント間の流れ | Mermaidまたは簡潔なフロー |
| 項目の対応、条件ごとの差、責務の比較 | 表 |
| データの正確な形、識別子の組み立て | JSONやtextなどのコード例 |
| 特に重要な制約、注意、補足 | Callout |
| 背景、理由、表や図だけでは伝わらない意味 | 短い文章 |

表現を選ぶときは、次を守る。

- 小さな節へ分け、見出しだけでも情報の階層が分かるようにする。
- 表や図が何を説明し、どの問いに答えるものかを、見出しまたは直前の短い導入から理解できるようにする。
- 表や図の目的がすでに明らかな場合は、見出しや内容を言い換えるだけの導入を追加しない。
- 文章が2〜3行以上続いて見える場合は、構造を見直す目安とする。文を機械的に分割せず、見出し、表、図、コード例、Calloutで構造を示せないか確認する。
- 文章、表、図のどれか1つへ一律に寄せず、内容に合う表現を選ぶ。
- 同じ情報を文章、表、図で繰り返さない。視覚表現は、関係、比較、順序など、文章だけでは見えにくい構造を示すために使う。
- 行数や文字数を目標にしない。必要な説明は残し、機械可読な正本の複製と意味のない言い換えを削る。
- ObsidianとGitHubの両方で、見出し、表、図、Callout、コード例の関係を追える状態にする。

この表現方針は、設計内容の厳密さを弱めるものではない。正確な規則と責務境界を保ったうえで、読み手が構造を先に理解できる形へ整理する。

### 4.6 `adr/`

重要な設計判断について、背景、検討した選択肢、決定、結果と影響を記録する。

ADRは、次のいずれかに該当する場合に作成する。

- 複数の現実的な選択肢がある。
- 変更コストが高い。
- 複数の機能やシステム全体へ影響する。
- 判断理由を残さなければ、同じ議論を繰り返す可能性がある。

局所的な実装詳細や、容易に変更できる小さな判断までADRにしない。

最小構成：

```markdown
# ADR-0001 — 判断の題名

## 背景

## 決定

## 検討した選択肢

## 結果と影響
```

判断が変わった場合は、採用済みADRを黙って書き換えて履歴を消さない。新しいADRを作成し、旧ADRを`superseded`へ変更する。

### 4.7 `experiments/`

仮説を実際に検証した記録を置く。

- 仮説
- 条件
- 手順
- 結果
- 失敗例
- 解釈
- 次の判断

例えば、チャンク条件、検索方式、Embedding model、rerankingなどを、同一の評価データで比較した記録を置く。

一般知識の解説や、単なる実装作業の記録は置かない。実験結果が重要な設計判断につながった場合は、ADRから該当する実験記録へリンクする。

### 4.8 `rules/`

RAGScopeの文書管理、Obsidianメタデータ、プロジェクト管理に関する規約を置く。

- `RAGScope文書管理規約.md`は、文書の配置、責務、正本、参照関係を定義する。
- `Obsidianメタデータ規約.md`は、Frontmatterのプロパティ、型、許容値を定義する。
- `RAGScopeプロジェクト管理規約.md`は、Roadmap、Milestone、Epic、Ticketの運用を定義する。

### 4.9 `project-management/`

Roadmap、Milestone、Epic、Ticketなど、開発作業の計画と進行記録を管理する。

要求や設計の現在内容を、プロジェクト管理文書だけに記載しない。具体的な運用は[RAGScopeプロジェクト管理規約](./RAGScopeプロジェクト管理規約.md)に従う。

### 4.10 `RAGScope開発運用ガイド.md`

RAGScopeのプロジェクト管理に関する具体的な操作手順を一元的に記載する。

- Obsidianテンプレートの初回設定と挿入方法
- Milestone・Epic・Ticketの作成順序
- Ticketへの着手、Branch作成、Pull Request、完了確認
- Epicの完了とMilestoneのリリース
- Ticketの移動・中止などの例外操作

定義、状態遷移、完了条件、命名規則などのルールは複製せず、各規約の該当箇所へリンクする。ツールや操作方法が変わった場合は本ガイドを更新し、規約には操作手順を追加しない。

### 4.11 `templates/`とリポジトリルートの`.github/`

`templates/`には、設計書およびMilestone・Epic・Ticketノートへ挿入するObsidianテンプレートを置く。テンプレートは、必須構造とFrontmatterの入力漏れを防ぎ、同じ種類の文書を一定の順序で読めるようにする作業支援ファイルである。テンプレートファイル自体を、実際の設計書、Milestone、Epic、Ticketとして扱わない。

設計書テンプレートは、機能設計書の冒頭に必要な目的、根拠、責務境界を揃え、基本設計から詳細設計まで必要な粒度を選択できる雛形とする。設計対象に不要な選択節は、作成した設計書から削除する。

リポジトリルートの`.github/pull_request_template.md`には、Pull Request作成時に記載する項目と完了確認を置く。規約本文の説明を複製せず、実際の作業時に確認する最小限の入力欄とチェック項目だけを記載する。

設計書テンプレートで必須とする構成と使用規則は本規約、Milestone・Epic・Ticketテンプレートは[RAGScopeプロジェクト管理規約](./RAGScopeプロジェクト管理規約.md)を正本とする。具体的な操作方法は[RAGScope開発運用ガイド](../RAGScope開発運用ガイド.md)、実際に挿入される雛形は各テンプレートファイルを正本とする。

## 5. 文書間の整合

- 要求定義は、RAGScope概要に示した目的、価値、対象範囲と矛盾しないこと。
- 設計書は、関連する要求を実現する内容とする。
- 文書間のリンクは、ナビゲーションまたは根拠の参照であり、リンク先の情報をリンク元が正本として保持することを意味しない。

## 6. 命名・リンク・表記規則

### 6.1 ファイル・フォルダ名

- 人が管理・閲覧するMarkdownノートには、内容を直接理解できる日本語ファイル名を使用する。
- 構造を表すフォルダ、コード、設定ファイル、機械生成物には英語名を使用する。
- 外部ツールが認識するファイル名は、そのツールの規約を優先する。
- 英語のフォルダ名は、原則として小文字のkebab-caseとする。
- ADRは`ADR-<4桁の連番> <日本語タイトル>.md`とする。
- Milestone、Epic、Ticketの命名はRAGScopeプロジェクト管理規約に従う。

### 6.2 見出し・本文

- MarkdownのH1は、その文書の名称を明確に表す。原則としてファイル名に対応させるが、README、ADR、Milestoneなど、別規約またはテンプレートで形式を定める文書は、その形式に従う。
- 説明文の基本言語は日本語とする。
- RAGScope固有のコンポーネント名と領域用語は、[システムアーキテクチャ「1.1 正式用語」](<../design/システムアーキテクチャ.md#1.1 正式用語>)を正本とし、人が読む文書では同じ意味に同じ正式用語を使用する。
- 実装言語は実装技術として記載し、コンポーネント名の代わりに使用しない。
- 正式用語の別名一覧を各文書へ複製しない。新しいRAGScope固有の用語を定義する場合や既存用語の意味を変更する場合は、システムアーキテクチャの正式用語を更新する。
- コード上の識別子、API名、型名、技術用語、引用は、必要に応じて原表記を使用する。

### 6.3 リンク

- 本文中のノート間リンクには、標準Markdownの相対リンクを使用する。
- リンクの解決はObsidianを優先し、GitHub上でも少なくともリンク先ファイルを参照できる形式を維持する。
- ファイル内の見出しへリンクする場合は、GitHubが生成するアンカーへ変換せず、`#`の後に実際の見出し名を見出し番号も含めて記載する。
- パスまたは見出し名に空白を含む場合は、リンク先全体を`<...>`で囲む。
- ファイルまたは見出しを移動・改名する場合は、同じ変更内で参照リンクを更新する。
- `milestone`・`epic`プロパティ内のリンク形式は、Obsidianメタデータ規約に従う。

例：

```markdown
[RAGScope概要](<../RAGScope概要.md>)
[文書処理設計](<../design/文書処理設計.md>)
[親子状態の整合](<./RAGScopeプロジェクト管理規約.md#6.1 親子状態の整合>)
```

### 6.4 Markdown表記

- FrontmatterはObsidianメタデータ規約に従う。
- Mermaid図内の改行には`<br>`を使用する。
- Obsidian固有の記法を使用する場合も、GitHub上の可読性を著しく損なわないようにする。
