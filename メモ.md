はい、**今の段階でチケットを再構築するのがよい**と思います。  
これは「最初のチケット作成が失敗だった」というより、**チケット一式を作ったことで、Epic・設計・実装の関係を初めて実物として検証できた**結果です。

コードを書いてから気づくより、ずっと安い手戻りです。むしろ今見つかってよかったやつです。

## 方針としては「Epic冒頭で初期設計」が自然です

おっしゃるとおり、設計書を最初から完全に作り切る必要はありません。

自然なのは次の流れです。

```mermaid
flowchart LR
    Epic["Epicの能力を定義"]
    Design["初期設計を作成"]
    Implement1["実装Ticket 1"]
    Update1["設計を更新"]
    Implement2["実装Ticket 2"]
    Update2["設計を更新"]
    Complete["Epic完了時に<br>現在設計との整合を確認"]

    Epic --> Design --> Implement1 --> Update1 --> Implement2 --> Update2 --> Complete
```

Epic冒頭の設計Ticketでは、後続Ticketが実装を開始できる程度まで決めます。

- 機能の責務
    
- 対象範囲・対象外
    
- 全体フロー
    
- コンポーネント間の境界
    
- 入出力
    
- データの概念構造
    
- 主要な不変条件
    
- エラーの基本方針
    
- 実装上の正本が何か
    

型名、関数名、正確なSQL、APIの全フィールドまで先回りして固定する必要はありません。正確な型はコード、DB定義はmigration、API schemaはOpenAPIを正本とする現在の方針とも両立します。

そして実装Ticketでは、

> 設計書を初めて作る

のではなく、

> 初期設計に従って実装し、実装で具体化・変更された現在設計を反映する

という役割にします。

## 前回の案を少し修正したいです

前回、Epic 2には設計Ticketを2件置く案を出しました。

- Embedding生成方式とAPIの設計
    
- データモデル設計
    

これは成立する案ですが、今回の話を踏まえると、**原則として各Epicの冒頭に1件の初期設計Ticketを置く**ほうが、シンプルで一貫しています。

### Epic 1

```text
固定Markdown文書の取り込みとチャンク化を設計する
↓
RS-0001 固定Markdownファイルを読み込む
↓
RS-0002 固定ルールでMarkdown文書をチャンク化する
```

初期設計の成果物：

```text
docs/design/文書処理設計.md
```

### Epic 2

```text
文書チャンクのEmbedding生成と保存を設計する
↓
RS-0003 Python AI Serviceで文書チャンクのEmbeddingを生成する
↓
RS-0004 Haskellから文書チャンクのEmbeddingを取得する
↓
RS-0005 PostgreSQLに保存するデータ構造を作成する
↓
RS-0006 文書チャンクとEmbeddingを保存する
```

初期設計の成果物：

```text
docs/design/Embedding生成設計.md
docs/design/データモデル設計.md
OpenAPIの初版
```

ここでは、Embedding modelの選定、文書用・質問用の入力規則、vector次元、HaskellとPythonの境界、DBへ保存する概念的なデータ構造までを決めます。

DBの正確なテーブル・カラム・制約はRS-0005で作るmigrationが正本なので、初期設計時点でSQLレベルまで完成させる必要はありません。

モデル選定に実測が必要になった場合だけ、設計Ticketの前に小さなExperimentや調査Ticketを追加します。

### Epic 3

```text
質問によるdense検索を設計する
↓
RS-0007 Python AI Serviceで質問文のEmbeddingを生成する
↓
RS-0008 Haskellから質問文のEmbeddingを取得する
↓
RS-0009 pgvectorでexact vector searchを実行する
↓
RS-0010 Haskell CLIから検索して表示する
```

初期設計の成果物：

```text
docs/design/検索設計.md
Embedding生成設計.mdの質問Embedding部分
OpenAPIの更新
```

> [!important]  
> 私の現在の推奨は、**v0.0では設計Ticketを3件追加する**形です。  
> 基本は「1 Epicにつき、冒頭に1件の初期設計Ticket」です。

もちろん、小さなEpicや既存機能の単純な拡張なら、専用の設計Ticketを省略しても構いません。機械的に必須にするのではなく、複数の実装Ticketが共通の設計判断へ依存する場合に設ける、という基準がよいです。

## 既存Ticketはどう扱うか

RS-0001〜RS-0010は、**IDを維持したまま内容を全面的に見直す**のがよいです。

現在の規約では、一度作成したTicketは原則削除せず、IDを変更・再利用しないことになっています。

したがって、

- RS-0001〜RS-0010は既存IDのまま改稿
    
- 新しい設計TicketはRS-0011以降で採番
    
- 実際の着手順はTicket ID順にしない
    
- 設計Ticketを完了してから、対応する既存実装Ticketへ進む
    

という扱いが安全です。

Ticket IDは識別子であって、実行順序ではありません。RS-0011がRS-0001より先に実行されても問題ありません。

RS-0001はすでに`in_progress`ですが、まだ実装未着手なら、内容を修正したうえでいったん作業を止め、Epic 1の設計Ticketを先に完了してから再開するのが現実的です。わざわざRS-0001を`cancelled`にして作り直すほどではありません。

## 今後の作り直しを減らす方法

一番大きな改善点は、**Ticketを作成した直後、着手する前に「Epic全体の実行計画レビュー」を挟むこと**です。

現在の開発運用ガイドは、概ね次の流れです。

```text
Epicを作成
→ Ticketを作成
→ Ticketへ着手
```

設計成果物やTicket間の依存関係を、セットとして確認する工程がありません。

ここに一段追加します。

```mermaid
flowchart LR
    Epic["Epicを作成"]
    Skeleton["Ticket構成案を作成"]
    Review["Epic実行計画をレビュー"]
    Detail["各Ticketを詳細化"]
    Start["最初のTicketへ着手"]

    Epic --> Skeleton --> Review --> Detail --> Start
```

### 1. Ticketを二段階で作る

いきなり各Ticketの詳細な本文を書きません。

最初は次だけを並べます。

- Ticketタイトル
    
- 主な成果物
    
- 前提となるTicket
    
- 更新する設計書・OpenAPI・migration
    
- 実行順序
    
- Epic全体の最終確認方法
    

この骨格をレビューしてから、目的・完了条件・対象外・実装メモを書き込みます。

今回の問題は、各Ticket単体はかなり丁寧だった一方で、**Ticket群全体の時系列を確認する工程が不足していた**ことです。

### 2. Epic計画時の確認項目を決める

各Epicについて、Ticketを詳細化する前に次を確認します。

- このEpicが完成すると、何ができるようになるか
    
- 新規作成・更新する設計書は何か
    
- 複数Ticketが共有する設計判断は何か
    
- 最初の実装Ticketが必要とする設計は揃っているか
    
- 後続Ticketで、前のTicketに必要な設計書を初めて作ろうとしていないか
    
- コード、OpenAPI、migrationなど、正確な仕様の正本は何か
    
- Epic全体をどのように実行・検証するか
    

特に、次の問いが強力です。

> [!question]  
> 後続Ticketが作成する成果物を、先行Ticketが暗黙に必要としていないか？

今回の`文書処理設計.md`や`検索設計.md`は、まさにここで検出できます。

### 3. 規約とガイドへ最小限のルールを追加する

新しいメタデータや複雑な管理項目は不要です。

`RAGScopeプロジェクト管理規約`には、原則として次を追加するのがよいと思います。

> 複数の実装Ticketが共通する機能設計を前提とする場合は、原則としてEpicの最初に、実装着手に必要な初期設計を作成または更新するTicketを設ける。後続の実装Ticketでは、実装によって具体化した現在設計を同じ設計書へ反映する。

`RAGScope開発運用ガイド`には、操作手順として、

1. Epicの設計成果物を確認する
    
2. Ticketタイトルと依存順を先に作る
    
3. Epic全体としてレビューする
    
4. 問題がなければ各Ticketを詳細化する
    
5. 最初のTicketへ着手する
    

を追加します。

プロジェクト管理規約は「原則」、開発運用ガイドは「具体的な手順」という現在の分離にも合っています。

## 作り直しを完全になくす必要はありません

チケットを一度作ってみないと、見えない依存関係はあります。

大事なのは、

- チケットを書き直さないこと
    

ではなく、

- 実装やDB schemaを作った後に、設計工程の不足へ気づかないこと
    

です。

今回の作り直しは、プロセス上かなり健全です。  
この経験をもとに、

> **Epic作成 → Ticket骨格作成 → Epic全体レビュー → Ticket詳細化 → 着手**

へ運用を一段進化させれば、今後は同種の全面的な作り直しはかなり減ると思います。